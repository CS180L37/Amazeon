import static org.junit.Assert.assertEquals;

import java.util.ArrayList;
import java.util.Arrays;

import org.junit.Test;

// Dashboard parent class has no methods to test
public class SellerDashboardTest extends TestUtils {
    // TODO: Need to figure out how this sorting works
    @Test
    public void testSortCustomersByNumItemsPurchased() {
        // assertEquals(this.nintendoDashboard.sort1(), new
        // ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
        // assertEquals(this.microsoftDashboard.sort1(),
        // new ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
        // assertEquals(this.sonyDashboard.sort1(),
        // new ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
    }

    @Test
    public void testSortProductsByNumProductSales() {
        // assertEquals(this.nintendoDashboard.sort2(), new
        // ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
        // assertEquals(this.microsoftDashboard.sort2(),
        // new ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
        // assertEquals(this.sonyDashboard.sort2(),
        // new ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
    }

    @Test
    public void testDisplayDashboard() {
        throw new UnsupportedOperationException("Unimplemented method 'testDisplayDashboard'");
    }
}