import static org.junit.Assert.assertEquals;

import java.util.ArrayList;
import java.util.Arrays;

import org.junit.Test;

// Dashboard parent class has no methods to test
public class CustomerDashboardTest extends TestUtils {

    // TODO: Need to figure out how this sorting works
    @Test
    public void testSortStoresByProductsSold() {
        // assertEquals(this.gamerDashboard.sort1(), new
        // ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
        // assertEquals(this.nintendoFanBoyDashboard.sort1(), new
        // ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
    }

    @Test
    public void testSortStoresByProductsPurchased() {
        // assertEquals(this.gamerDashboard.sort2(), new
        // ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
        // assertEquals(this.nintendoFanBoyDashboard.sort2(), new
        // ArrayList<Store>(Arrays.asList(this.gamestop, this.bestBuy)));
    }

    @Test
    public void testDisplayDashboard() {
        throw new UnsupportedOperationException("Unimplemented method 'testDisplayDashboard'");
    }
}