package screens;
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

public class LoginGUI {
    public static void main(String[] args) {
        /*
        psuedocode --> JOptionPane

        Image image; // the canvas
        Graphics2D graphics2D; // don't need if not using paint
        int curx; // might not need
        int cury; // might not need
        int oldx; // might not need
        int oldy; // might not need

        JTextField emailTextField; //email input text field
        JTextField passwordTextField; //password input text field
        JButton loginButton; //login button
        JButton createAccountButton; //should display createAccountGUI

        String email;
        String password;

        ActionListener actionListener = new ActionListener() {
            public void actionPerformed(ActionEvent e) {
                if(e.getSource() == loginButton) {
                    email = String.valueOf(emailTextField.getText());
                    password = String.valueOf(passwordTextField.getText());
                }
                if(e.getSource() == createAccountButton) {
                    //call and display createAccountGUI
                }
            }
        }


        public void run() {
            JFrame frame = new JFrame("Login");

            Container content = frame.getContentPane();

            content.setLayout(new BorderLayout());

        }
        */
    }
}
